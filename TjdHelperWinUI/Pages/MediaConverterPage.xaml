<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TjdHelperWinUI.Pages.MediaConverterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TjdHelperWinUI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Expander
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                ExpandDirection="Down"
                Header="Audio To PCM"
                IsExpanded="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            Grid.Column="0"
                            MaxWidth="300"
                            Command="{Binding ChooseAudioPathCommand, Mode=TwoWay}"
                            Content="选择音频文件" />
                        <TextBox
                            Grid.Column="1"
                            Margin="5,0,0,0"
                            HorizontalAlignment="Stretch"
                            PlaceholderText="pcm格式音频文件Path"
                            Text="{Binding StrAudioFilePath}"
                            TextWrapping="Wrap" />
                        <Button
                            Grid.Column="2"
                            MaxWidth="300"
                            Margin="5,0,0,0"
                            Command="{Binding ConvertToPCMCommand}"
                            Content="转换成PCM格式" />
                        <Button
                            Grid.Column="3"
                            MaxWidth="300"
                            Margin="5,0,0,0"
                            Command="{Binding OpenFilePathCommand}"
                            Content="打开文件夹" />
                    </Grid>

                    <TextBox
                        Grid.Row="1"
                        MinWidth="50"
                        Margin="0,10,0,0"
                        PlaceholderText="转换详情"
                        Text="{Binding StrPCMConvertLog}"
                        TextWrapping="Wrap" />
                </Grid>
            </Expander>

        </Grid>
    </ScrollViewer>
</Page>
