<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TjdHelperWinUI.Pages.MediaConverterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TjdHelperWinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollMode="Auto"
       VerticalScrollBarVisibility="Auto"
       HorizontalScrollMode="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Expander Grid.Row="0" IsExpanded="True" ExpandDirection="Down" Header="Audio To PCM" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Content="选择音频文件" Command="{Binding ChooseAudioPathCommand,Mode=TwoWay}" MaxWidth="300" Grid.Column="0"/>
                        <TextBox Margin="5 0 0 0" PlaceholderText="pcm格式音频文件Path" Text="{Binding StrAudioFilePath}" HorizontalAlignment="Stretch" Grid.Column="1" TextWrapping="Wrap"/>
                        <Button Content="转换成PCM格式" Command="{Binding ConvertToPCMCommand}" MaxWidth="300" Margin="5 0 0 0" Grid.Column="2"/>
                        <Button Content="打开文件夹" Command="{Binding OpenFilePathCommand}" MaxWidth="300" Margin="5 0 0 0" Grid.Column="3"/>
                    </Grid>

                    <TextBox MinWidth="50" PlaceholderText="转换详情" Text="{Binding StrPCMConvertLog}" Grid.Row="1" TextWrapping="Wrap" Margin="0 10 0 0"/>
                </Grid>
            </Expander>

        </Grid>
    </ScrollViewer>
</Page>
