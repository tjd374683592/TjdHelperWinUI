<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TjdHelperWinUI.Pages.FileHelperPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TjdHelperWinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollMode="Auto"
   VerticalScrollBarVisibility="Auto"
   HorizontalScrollMode="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Expander Grid.Row="0" IsExpanded="True" ExpandDirection="Down" Header="File Split" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Content="文件选择" Command="{Binding ChooseFilePathCommand,Mode=TwoWay}" MaxWidth="300" Grid.Column="0"/>
                        <TextBox Margin="5 0 0 0" PlaceholderText="文件路径 Path" Text="{Binding ToSplitFilePath}" HorizontalAlignment="Stretch" Grid.Column="1" TextWrapping="Wrap"/>
                        <TextBlock Text="分片大小：" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15 0 0 0"/>
                        <TextBox Margin="5 0 0 0" PlaceholderText="单位MB" Text="{Binding SplitFileSize,Mode=TwoWay}" HorizontalAlignment="Stretch" Grid.Column="3" TextWrapping="Wrap" MinWidth="100"/>
                        <Button Content="开始分片" Command="{Binding StartFileSplitCommand}" MaxWidth="300" Margin="5 0 0 0" Grid.Column="4"/>
                        <Button Content="打开文件夹" Command="{Binding OpenSplitFilePathCommand}" MaxWidth="300" Margin="5 0 0 0" Grid.Column="5"/>
                    </Grid>

                    <TextBlock Text="分片详情：" Grid.Row="1" Margin="0 10 0 0"/>
                    <TextBox MinWidth="50" PlaceholderText="Log：" Text="{Binding SplitDetailsContent}" Grid.Row="2" TextWrapping="Wrap" Margin="0 10 0 0"/>

                    <ProgressBar Grid.Row="3" Margin="0 10 0 0" Foreground="Azure" Background="{ThemeResource SystemControlBackgroundBaseLowBrush}" Visibility="{Binding SplitProgressVisibility}" Value="{Binding SplitProgressValue}"/>
                </Grid>
            </Expander>
        </Grid>
    </ScrollViewer>
</Page>
