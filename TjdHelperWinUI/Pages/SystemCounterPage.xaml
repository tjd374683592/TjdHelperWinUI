<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TjdHelperWinUI.Pages.SystemCounterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TjdHelperWinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- CPU -->
        <Grid Grid.Row="0">
            <lvc:CartesianChart x:Name="CpuChart"/>
            <TextBlock x:Name="CpuText" Foreground="White" FontWeight="Bold" FontSize="14"
                   HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"/>
        </Grid>

        <!-- 内存 -->
        <Grid Grid.Row="1">
            <lvc:CartesianChart x:Name="MemChart"/>
            <TextBlock x:Name="MemText" Foreground="White" FontWeight="Bold" FontSize="14"
                   HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"/>
        </Grid>

        <!-- 磁盘 -->
        <Grid Grid.Row="2">
            <lvc:CartesianChart x:Name="DiskChart"/>
            <TextBlock x:Name="DiskText" Foreground="White" FontWeight="Bold" FontSize="14"
                   HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"/>
        </Grid>

        <!-- 网络 -->
        <Grid Grid.Row="3">
            <lvc:CartesianChart x:Name="NetChart"/>
            <TextBlock x:Name="NetText" Foreground="White" FontWeight="Bold" FontSize="14"
                   HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0"/>
        </Grid>
    </Grid>
</Page>
