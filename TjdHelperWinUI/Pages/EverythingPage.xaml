<Page
    x:Class="TjdHelperWinUI.Pages.EverythingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:TjdHelperWinUI.Models"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer
        HorizontalScrollMode="Disabled"
        VerticalScrollBarVisibility="Auto"
        VerticalScrollMode="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Expander
                Grid.Row="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                ExpandDirection="Down"
                Header="Audio To PCM"
                IsExpanded="True">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  搜索输入  -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox
                            Grid.Column="0"
                            Margin="0,0,5,0"
                            KeyDown="SearchTextBox_KeyDown"
                            PlaceholderText="Search Everything..."
                            Text="{Binding SearchKeywords, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        <Button
                            Grid.Column="1"
                            MinWidth="80"
                            Command="{Binding StartSearchingCommand}"
                            Content="search" />
                    </Grid>

                    <controls:DataGrid
                        Grid.Row="1"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"
                        AutoGenerateColumns="False"
                        CanUserResizeColumns="True"
                        HeadersVisibility="Column"
                        ItemsSource="{Binding SearchResults}"
                        SelectionMode="Single">

                        <controls:DataGrid.Columns>
                            <controls:DataGridTextColumn
                                Width="60"
                                Binding="{Binding Id}"
                                Header="Id" />
                            <controls:DataGridTextColumn
                                Width="200"
                                Binding="{Binding Name}"
                                Header="Name" />
                            <controls:DataGridTextColumn
                                Width="*"
                                Binding="{Binding Directory}"
                                Header="Directory" />
                            <controls:DataGridTextColumn
                                Width="120"
                                Binding="{Binding Size}"
                                Header="Size" />
                            <controls:DataGridTextColumn
                                Width="180"
                                Binding="{Binding DateModified}"
                                Header="Date Modified" />
                        </controls:DataGrid.Columns>

                        <!--  右键菜单  -->
                        <controls:DataGrid.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem
                                    Command="{Binding DataContext.OpenFileCommand, ElementName=DataGrid}"
                                    CommandParameter="{Binding}"
                                    Text="打开文件" />
                                <MenuFlyoutItem
                                    Command="{Binding DataContext.OpenDirectoryCommand, ElementName=DataGrid}"
                                    CommandParameter="{Binding}"
                                    Text="打开目录" />
                                <MenuFlyoutSeparator />
                                <MenuFlyoutItem
                                    Command="{Binding DataContext.DeleteCommand, ElementName=DataGrid}"
                                    CommandParameter="{Binding}"
                                    Text="删除" />
                            </MenuFlyout>
                        </controls:DataGrid.ContextFlyout>
                    </controls:DataGrid>

                </Grid>
            </Expander>
        </Grid>
    </ScrollViewer>
</Page>
