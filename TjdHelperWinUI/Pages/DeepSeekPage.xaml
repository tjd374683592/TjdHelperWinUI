<Page
    x:Class="TjdHelperWinUI.Pages.DeepSeekPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdcontrols="using:CommunityToolkit.Labs.WinUI.MarkdownTextBlock"
    Loaded="Page_Loaded"
    mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="PivotHeaderItem">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Medium" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Pivot>
            <PivotItem Header="Text">
                <Border
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SystemAccentColor}"
                    BorderThickness="1"
                    CornerRadius="3">

                    <TextBox
                        x:Name="tbDeepSeekResponse"
                        AcceptsReturn="True"
                        IsReadOnly="True"
                        ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        TextWrapping="Wrap" />

                </Border>
            </PivotItem>
            <PivotItem Header="Markdown">
                <Border
                    x:Name="SideContent"
                    Padding="5"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SystemAccentColor}"
                    BorderThickness="1"
                    CornerRadius="3">
                    <ScrollViewer Grid.Column="0">

                        <mdcontrols:MarkdownTextBlock
                            x:Name="MarkdownTextBlock1"
                            Config="{x:Bind LiveMarkdownConfig, Mode=OneTime}"
                            Text="{Binding StrShowFinMarkdown, Mode=TwoWay}" />

                    </ScrollViewer>
                </Border>
            </PivotItem>
        </Pivot>

        <Grid Grid.Row="1" Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox
                x:Name="tbInput"
                MaxHeight="200"
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                AcceptsReturn="True"
                PlaceholderText="询问任何问题"
                ScrollViewer.VerticalScrollBarVisibility="Auto"
                Text="{Binding StrQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap">
                <TextBox.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Enter" Invoked="OnEnterKeyInvoked" />
                </TextBox.KeyboardAccelerators>
            </TextBox>

            <Button
                Grid.Column="1"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Command="{Binding ChatCompletionCommand}">
                <FontIcon Glyph="{Binding SendButtonGlyph}" />
            </Button>

            <Button
                Grid.Column="2"
                Margin="5,0,0,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Command="{Binding ClearChatCommand}"
                Content="新会话" />
        </Grid>
    </Grid>
</Page>