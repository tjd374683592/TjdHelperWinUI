<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TjdHelperWinUI.Pages.MarkdownPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TjdHelperWinUI.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mdcontrols="using:CommunityToolkit.Labs.WinUI.MarkdownTextBlock"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Expander
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            ExpandDirection="Down"
            Header="Markdown"
            IsExpanded="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    x:Name="SideContent"
                    MinWidth="500"
                    MaxWidth="1000"
                    Margin="5"
                    Padding="5"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource SystemAccentColor}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <ScrollViewer Grid.Column="0">

                        <mdcontrols:MarkdownTextBlock
                            x:Name="MarkdownTextBlock1"
                            Config="{x:Bind LiveMarkdownConfig, Mode=OneTime}"
                            Text="{x:Bind MarkdownTextBox.Text, Mode=OneWay}" />

                    </ScrollViewer>
                </Border>
                <controls:ContentSizer Grid.Column="1" TargetControl="{x:Bind SideContent}" />

                <ScrollViewer Grid.Column="2">
                    <TextBox
                        x:Name="MarkdownTextBox"
                        Margin="5"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        AcceptsReturn="True"
                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                        TextWrapping="Wrap" />
                </ScrollViewer>
            </Grid>
        </Expander>
    </Grid>
</Page>